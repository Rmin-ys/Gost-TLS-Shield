# 🛡️ Gost-TLS-Shield
**یک اسکریپت هوشمند و تعاملی برای ایجاد تانل امن TLS جهت دور زدن فیلترینگ و DPI.**

این پروژه با استفاده از ابزار قدرتمند **Gost**، یک پل ارتباطی رمزنگاری شده بین سرور ایران و خارج شما برقرار می‌کند. ترافیک در این روش کاملاً در لایه‌ی **TLS** مخفی می‌شود تا توسط سیستم‌های بازرسی عمیق پکت (DPI) شناسایی نشود.

---

## ✨ ویژگی‌های برجسته
* ✅ **نصب کاملاً تعاملی:** پرسش مرحله‌به‌مرحله برای تنظیم پورت‌ها و آی‌پی‌ها.
* 🚀 **بهینه‌سازی BBR:** فعال‌سازی خودکار الگوریتم BBR برای افزایش چشمگیر سرعت و کاهش پکت‌لاس.
* 🔐 **لایه امن TLS:** مخفی‌سازی ترافیک پشت پروتکل استاندارد HTTPS.
* ⚙️ **مدیریت سرویس (Systemd):** اجرای خودکار تانل با روشن شدن سرور و قابلیت راه اندازی مجدد در صورت بروز خطا.
* 🖥️ **پشتیبانی از معماری‌های مختلف:** شناسایی خودکار پردازنده‌های x86 و ARM.

---

## 🚀 نحوه نصب و اجرا

برای راه‌اندازی، دستور زیر را در ترمینال سرور (هم ایران و هم خارج) کپی و اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Rmin-ys/Gost-TLS-Shield/main/ys-gost-tls-shield)
